#ifndef AUTOMATED_H
#define AUTOMATED_H
#include <stdbool.h>

void  automationmanager();
void idleFunc();
void automatedTimer(int val);
bool isspeed =true;
bool isautomated = false;
typedef enum {
    //py buradan çeksin
    LINEARSCAN,
	BASICAUTOMATED,
    USERINPUT,
    MOUSEMOVE,
    ALLRANDOM,
    // ... başka state'ler eklersen buraya
} AutonomDirection;



 #define SETDIRECTION LINEARSCAN

AutonomDirection currentdireciton = SETDIRECTION; // Başlangıç yönü
const char* getDirectionName(AutonomDirection dir);

#endif